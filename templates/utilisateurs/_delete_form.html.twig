<form method="post" action="{{ path('utilisateurs_delete', {'id': utilisateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.id) }}">
    <div class="d-grid gap-2">
    <button class="btn btn-primary">Supprimer</button>
    </div>
</form>
